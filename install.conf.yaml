- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.config/nvim: vim
    ~/.zsh: zsh
    ~/.zshrc: zsh/zshrc
    ~/.p10k.zsh: zsh/themes/p10k.zsh
    ~/.i3: i3
    ~/.tmux.conf: tmux/tmux.conf
    ~/.terminfo: terminfo

